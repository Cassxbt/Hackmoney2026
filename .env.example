# AgentPay Environment Variables

# Private key for the agent's EOA (Ethereum account)
# Get this from your wallet or generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AGENT_PRIVATE_KEY=0x...

# EOA address corresponding to the private key above
# This is derived from AGENT_PRIVATE_KEY
AGENT_ADDRESS=0x...

# Network configuration (testnet or mainnet)
NETWORK=testnet

# Optional: Circle API keys (if using Gateway REST API)
# Get these from https://developers.circle.com/
# CIRCLE_API_KEY=...
